{# empty Twig template #}
{% extends 'base.html.twig' %}

{% block title %} Ports par types{% endblock %}
{% block body %}
  <div class="container" style="padding-top:10px;">
    <table class="table table-striped table-dark">
      <thead class="thead-dark">
        <tr>
          <th> Indicatif</th>
          <th> Nom </th>
          <th> Pays </th>
          <th> Actions </th>
        </tr>
      </thead>
      {% for port in ports %}
        <tr>
          <td>{{ port.indicatif}}</td>
          <td>{{ port.nom }}</td>
          <td>{{ port.nom }}</td>
        </tr>
      {% endfor %}
    </table>
  </div>
{% endblock %}